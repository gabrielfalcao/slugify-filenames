[project]
name = "slugify-filenames-e2e-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.2",
]


[project.scripts]
genfiles = "genfiles:main"
